# Use GitLab CI with Jenkinsfile Support

GitLab CI will be the primary pipeline-as-code definition, with a Jenkinsfile added when the instructor needs Jenkins-specific verification. This keeps one clear source of truth for automated build, test, Docker image, and E2E stages while still satisfying the course tooling requirement.
