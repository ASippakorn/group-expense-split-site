# Maintain OpenAPI with CI Validation

The project will start with a checked-in `docs/api/openapi.yaml` and validate it in CI. Once API routes stabilize, the team should either generate documentation from code or add tooling that checks the OpenAPI contract stays synchronized with the implemented Fiber routes.
