# Use Deterministic Minimum Suggested Transfers

Suggested Transfers will minimize the number of repayments needed to settle current Balances and use deterministic ordering for ties. This keeps the feature useful for real groups while making the calculation predictable enough for tests and user explanations.
