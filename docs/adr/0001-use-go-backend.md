# Use Go for the Backend

We will build the backend in Go instead of Spring Boot because the project owner confirmed there is no enforced backend-language constraint for this group, and Go keeps the API service small, fast to test, and easy to package for Docker and Kubernetes. This is a deliberate deviation from the project brief's default Spring Boot wording, so future contributors should not replace Go with Spring Boot unless the course requirement is clarified differently.
