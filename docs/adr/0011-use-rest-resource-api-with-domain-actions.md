# Use REST Resource API with Domain Actions

The API will use REST-style resources for ordinary create, read, update, and delete workflows, with explicit action endpoints for domain commands such as recording a Settlement or exporting a PDF. This gives the frontend predictable resource paths without forcing command-shaped behavior into awkward CRUD endpoints.
