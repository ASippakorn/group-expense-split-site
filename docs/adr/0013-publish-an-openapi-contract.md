# Publish an OpenAPI Contract

The backend will expose or generate an OpenAPI contract so the React frontend, tests, and reviewers have a clear description of the REST API. Keeping this contract visible early reduces ambiguity as requirements evolve through the semester.
