# Use Splitr as the Project Name

The project will use `splitr` for code, package, and module naming and `Splitr` as the UI title. This gives the app a short stable name that works in repository paths, Docker image names, API labels, and user-facing screens.
