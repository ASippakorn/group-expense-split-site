# Use Playwright for E2E Tests

End-to-end tests will use Playwright because it works well with React, can drive the browser against Docker Compose environments, and fits CI execution. The first E2E tests will cover the core user story rather than broad visual coverage.
