# Use Argon2id for Passwords

User passwords will be hashed with Argon2id and per-user salts. This gives the local application a modern password storage baseline without depending on an external identity provider.
