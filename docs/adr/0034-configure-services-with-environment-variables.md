# Configure Services with Environment Variables

Services will read configuration from environment variables, with `.env.example` documenting local defaults and Docker Compose loading development values. This keeps local, CI, and Kubernetes configuration aligned without committing secrets.
