# Use a Temporary GitHub Module Path

The Go module will start as `github.com/your-org/splitr` until the final GitHub organization and repository are created. This makes imports stable during scaffolding while keeping the replacement obvious.
