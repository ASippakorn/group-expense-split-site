# Use pnpm for the Web App

The React app will use pnpm for package management because it is fast, reproducible, and available in the bundled workspace runtime. The repository will keep pnpm workspace configuration at the root so frontend scripts can run predictably in local development and CI.
