# Use Plain Kubernetes Manifests First

The Minikube deployment will start with plain Kubernetes manifests instead of Helm. The project needs transparent deployment files for coursework and review, and Helm can be introduced later only if repeated environment-specific configuration becomes painful.
