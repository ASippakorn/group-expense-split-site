# Use Standard Local Development Ports

Local development will use port `5173` for the React web app, `8080` for the Go API, and `5432` for PostgreSQL. These defaults match common Vite, API, and database conventions and are documented through Compose and environment examples.
