# Keep Email as Phase 1 Login Identity

Phase 1 will use email as the unique login identity and will not support changing it. This keeps registration, login, session lookup, and Participant invitation simple while leaving email-change workflows for a later phase if needed.
