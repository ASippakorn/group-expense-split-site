# Run the App with Docker Compose

Docker Compose will run PostgreSQL, the Go API, and the React web app for the first runnable environment. This gives instructors and teammates a single local command path while still allowing developers to run individual services directly when needed.
