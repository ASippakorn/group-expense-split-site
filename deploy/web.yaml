apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: splitr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: splitr-web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: splitr
spec:
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
