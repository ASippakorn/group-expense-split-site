apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: splitr
type: Opaque
stringData:
  POSTGRES_USER: splitr
  POSTGRES_PASSWORD: splitr
  POSTGRES_DB: splitr
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: splitr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: postgres-secret
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: splitr
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
